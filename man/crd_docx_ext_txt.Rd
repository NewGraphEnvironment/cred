% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx.R
\name{crd_docx_ext_txt}
\alias{crd_docx_ext_txt}
\title{Extract text from a Word document as a tidy tibble}
\usage{
crd_docx_ext_txt(docx_path)
}
\arguments{
\item{docx_path}{\code{character(1)} path to a \code{.docx} file.}
}
\value{
A \link[tibble:tibble]{tibble} with columns:
\itemize{
\item \code{doc_index} (\code{integer}) — paragraph index in the document.
\item \code{content_type} (\code{character}) — \code{"paragraph"}, \code{"table cell"}, etc.
\item \code{style_name} (\code{character}) — Word style (e.g. \code{"Heading 1"},
\code{"Normal"}).
\item \code{text} (\code{character}) — paragraph text.
\item \code{level} (\code{integer}) — heading level (\code{NA} for body text).
}
}
\description{
Reads a \code{.docx} file using \code{\link[officer:read_docx]{officer::read_docx()}} and returns the full
document content as a tidy \link[tibble:tibble]{tibble}. Each row is one
paragraph or table cell, with the heading style preserved so callers
can filter by section.
}
\examples{
\dontrun{
doc <- crd_docx_ext_txt("background/price_etal2026.docx")
dplyr::filter(doc, style_name == "Heading 1")
}
}
