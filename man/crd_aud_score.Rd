% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_score}
\alias{crd_aud_score}
\title{Score audit rows by review priority}
\usage{
crd_aud_score(
  audit_file,
  env = parent.frame(),
  ignore_years = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV.}

\item{env}{\code{environment} in which to evaluate inline R expressions,
passed to \code{\link[=crd_aud_eval_inline]{crd_aud_eval_inline()}} when needed. Default
\code{parent.frame()}.}

\item{ignore_years}{\code{logical(1)} if \code{TRUE} (default), exclude standalone
4-digit years (1900--2099) from numeric claim detection. Years like
"since 2020" are date references, not statistics.}

\item{overwrite}{\code{logical(1)} if \code{TRUE}, rescore rows that already have
\code{review_score}. Default \code{FALSE}.}
}
\value{
The updated audit data frame, invisibly. Writes to \code{audit_file}.
}
\description{
Adds \code{review_score} (1--6) and \code{review_flag} columns to the audit CSV.
Lower scores indicate higher review priority (more likely hallucinated or
unsupported). Rows already scored are skipped unless \code{overwrite = TRUE}.
}
\details{
If \code{paraphrase_eval} is not yet populated and paraphrases contain inline R
expressions, \code{\link[=crd_aud_eval_inline]{crd_aud_eval_inline()}} is called automatically with \code{env}.
}
\section{Score meanings}{

\describe{
\item{1}{\code{no_match}, or \code{auto} with numeric claim absent from quote}
\item{2}{\code{NA} (no source), abstract with numeric claim, auto with no
quote, or very weak prose overlap}
\item{3}{\code{abstract_match} qualitative, weak prose overlap, or no tokens}
\item{4}{\code{auto} numeric partial match or moderate prose overlap}
\item{5}{\code{auto} strong numeric + prose match}
\item{6}{Human-reviewed (\code{yes}, \code{no}, \code{corrected}, \code{context})}
}
}

\examples{
\dontrun{
# After verify_all and verify_abstract:
crd_aud_score("background/citation_audit.csv")

# With project variables for inline R evaluation:
optimum_morice <- 18175
crd_aud_score("background/citation_audit.csv")
}
}
