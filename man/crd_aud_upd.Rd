% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_upd}
\alias{crd_aud_upd}
\title{Update an existing audit CSV preserving manual columns}
\usage{
crd_aud_upd(
  out_file = "citation_audit.csv",
  rmd_dir = ".",
  pattern = "^[0-9]{4}-.*\\\\.Rmd$",
  exclude = "(references|session-info|report-change-log)"
)
}
\arguments{
\item{out_file}{\code{character(1)} path to the existing audit CSV.}

\item{rmd_dir}{\code{character(1)} directory containing Rmd files.
Default \code{"."}.}

\item{pattern}{\code{character(1)} regex to select Rmd files.
Default \code{"^[0-9]{4}-.*\\\\.Rmd$"}.}

\item{exclude}{\code{character(1)} regex for files to skip.
Default skips references, session-info, and change-log chapters.}
}
\value{
Invisibly returns the updated \link[tibble:tibble]{tibble}.
}
\description{
Re-extracts citations from Rmd files and merges with an existing audit CSV,
preserving any manually filled \code{quote}, \code{claim_type}, \code{page_or_section},
\code{verified}, and \code{notes} values. New rows are appended with blank manual
columns; rows no longer present in the Rmd are retained but flagged.
}
\examples{
\dontrun{
crd_aud_upd("background/citation_audit.csv")
}
}
