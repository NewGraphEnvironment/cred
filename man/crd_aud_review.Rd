% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{crd_aud_review}
\alias{crd_aud_review}
\title{Launch an interactive review app for the citation audit CSV}
\usage{
crd_aud_review(audit_file, launch_browser = TRUE)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV produced by
\code{\link[=crd_aud_write]{crd_aud_write()}}.}

\item{launch_browser}{\code{logical(1)} open the app in the system browser.
Default \code{TRUE}.}
}
\value{
Called for side effects. Writes to \code{audit_file} on Save.
}
\description{
Opens a local Shiny app in your browser. Click any row to see the full
\code{paraphrase} and \code{quote} side by side, set \code{verified} from a dropdown,
add \code{notes}, and save changes back to the CSV. The CSV remains the source
of truth — git history is preserved.
}
\section{Workflow}{

\enumerate{
\item Filter to \code{"auto"} rows (default on open).
\item Click a row — full paraphrase and quote appear below.
\item Read both, set \code{verified} to \code{yes} / \code{no} / \code{corrected} / \code{context}.
\item Add a \code{notes} entry if the claim needs correction or explanation.
\item Click \strong{Update row} to apply, then \strong{Save to CSV} when done.
}
}

\examples{
\dontrun{
crd_aud_review("background/citation_audit.csv")
}
}
