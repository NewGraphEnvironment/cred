% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_fmt_xlsx}
\alias{crd_aud_fmt_xlsx}
\title{Format an audit CSV as a readable Excel workbook}
\usage{
crd_aud_fmt_xlsx(audit_file, out_file = NULL)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV produced by
\code{\link[=crd_aud_write]{crd_aud_write()}}.}

\item{out_file}{\code{character(1)} output \code{.xlsx} path. Default replaces
the \code{.csv} extension with \code{.xlsx} in the same directory.}
}
\value{
Invisibly returns \code{out_file}.
}
\description{
Writes a \code{.xlsx} version of the audit CSV with formatting optimised for
manual review: fixed row height (so rows don't expand to full paragraph
height), wide columns for \code{paraphrase} and \code{quote}, frozen header row,
auto-filter, and colour-coded \code{verified} status cells.
}
\details{
Open the \code{.xlsx} in Excel or Numbers. Click any \code{paraphrase} or \code{quote}
cell to read the full text in the formula bar. Set \code{verified} to
\code{yes} / \code{no} / \code{corrected} directly in the workbook, then save and
re-import edits to the CSV manually, or continue editing the CSV directly.
}
\examples{
\dontrun{
crd_aud_fmt_xlsx("background/citation_audit.csv")
}
}
