% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{crd_store_push}
\alias{crd_store_push}
\title{Push a ragnar evidence store and merge it into the shared manifest}
\usage{
crd_store_push(
  store_path,
  source = getOption("cred.store_source"),
  name = NULL,
  profile = Sys.getenv("AWS_PROFILE"),
  built_by = "cred::crd_store_push()",
  dry_run = FALSE,
  create_manifest = FALSE,
  allow_model_mismatch = FALSE,
  max_retries = 3L
)
}
\arguments{
\item{store_path}{\code{character(1)} path to the \code{.duckdb} store to upload.}

\item{source}{\code{character(1)} destination URI holding the stores and
\code{log.json}. Defaults to \code{getOption("cred.store_source")}, then
\code{CRED_STORE_SOURCE}. Shape \verb{s3://<bucket>/<prefix>/}.}

\item{name}{\code{character(1)} store name in the manifest. Defaults to the file
name without its extension.}

\item{profile}{\code{character(1)} AWS profile. Default \code{AWS_PROFILE}.}

\item{built_by}{\code{character} what produced the store, recorded in the entry.}

\item{dry_run}{\code{logical(1)} print the merged manifest and upload nothing.
Default \code{FALSE}.}

\item{create_manifest}{\code{logical(1)} allow writing a manifest where none
exists. Default \code{FALSE}. The create path is still conditional
(\verb{--if-none-match "*"}), so two simultaneous first pushes cannot silently
overwrite one another.}

\item{allow_model_mismatch}{\code{logical(1)} push despite a differing embedding
model. Default \code{FALSE}.}

\item{max_retries}{\code{integer(1)} conditional-write retries before giving up.
Default \code{3L}.}
}
\value{
Invisibly, the merged manifest as a \code{list}.
}
\description{
Uploads a built store and records it in the bucket's single \code{log.json}
manifest, \strong{merging} rather than replacing. The manifest describes every
store in the bucket, so a push built from the current run alone silently
orphans the others — the pull side then reports "not in log.json" for a file
plainly present. That failure has been observed in practice.
}
\details{
Three deliberate refusals, each guarding a way the manifest can be corrupted:
\itemize{
\item \strong{An unreadable manifest aborts the push.} Only a \emph{confirmed} absence —
bucket reachable, object missing — is treated as "no manifest yet", and
even then \code{create_manifest = TRUE} is required. A wrong prefix in
\code{cred.store_source} produces exactly the same 404 as a genuine first push.
\item \strong{A concurrent push cannot clobber this one.} The manifest is written
conditional on the ETag read at the start; a competing write fails the
precondition, and the merge is retried against the newer manifest.
\item \strong{A store with an unflushed WAL is refused}, since its md5 does not
describe what a puller would open.
}

The store binary is uploaded before the manifest that describes it, so a
failed manifest write leaves the new binary in place under the old md5 and
pulls of that store fail until the push is re-run. Both failure messages say
so. Removing the window entirely means content-addressed keys
(\verb{<name>-<md5>.duckdb}), which is a change to the shared bucket layout and
belongs with the infrastructure rather than here.

Building the store is \code{\link[=crd_store_build]{crd_store_build()}}; reading it back is
\code{\link[=crd_store_connect]{crd_store_connect()}}. Bucket policy, IAM and retention are infrastructure
concerns and deliberately live outside this package.
}
\examples{
\dontrun{
options(cred.store_source = "s3://<bucket>/<prefix>/")

# Always dry-run first — prints the merged manifest, uploads nothing.
crd_store_push("data/rag/vca_refs.duckdb", dry_run = TRUE)

crd_store_push("data/rag/vca_refs.duckdb")
}
}
