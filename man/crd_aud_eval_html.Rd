% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_eval_html}
\alias{crd_aud_eval_html}
\title{Backfill paraphrase_eval from rendered bookdown HTML}
\usage{
crd_aud_eval_html(audit_file, html_dir, overwrite = FALSE)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV.}

\item{html_dir}{\code{character(1)} path to the rendered HTML output directory
(e.g., from \code{yaml::read_yaml("_bookdown.yml")$output_dir}).}

\item{overwrite}{\code{logical(1)} if \code{TRUE}, re-evaluate rows that already have
\code{paraphrase_eval}. Default \code{FALSE}.}
}
\value{
The updated audit data frame, invisibly. Writes to \code{audit_file}.
}
\description{
Paraphrases containing inline R expressions (\code{`r expr`}) cannot be
scored accurately because the R variables are only available during
bookdown rendering. This function reads the rendered HTML output,
matches each paraphrase to its rendered paragraph, and extracts the
evaluated values to populate \code{paraphrase_eval}.
}
\examples{
\dontrun{
yml <- yaml::read_yaml("_bookdown.yml")
crd_aud_eval_html("qa/citation_audit.csv", html_dir = yml$output_dir)
}
}
\seealso{
\code{\link[=crd_aud_eval_inline]{crd_aud_eval_inline()}} for evaluating inline R in an R session,
\code{\link[=crd_aud_score]{crd_aud_score()}} which uses \code{paraphrase_eval} for scoring.
}
