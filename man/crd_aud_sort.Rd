% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_sort}
\alias{crd_aud_sort}
\title{Sort an audit CSV for review and write it back}
\usage{
crd_aud_sort(audit_file, by = c("report", "status", "key"))
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV.}

\item{by}{\code{character(1)} sort strategy. One of:
\itemize{
\item \code{"report"} — restore to original report order (\code{sort_index}).
\item \code{"status"} — group by verification status, worst-first, then
\code{sort_index} within each group.
\item \code{"key"} — alphabetical by \code{citation_key}, then \code{sort_index}.
}}
}
\value{
Invisibly returns the sorted \link[tibble:tibble]{tibble}.
}
\description{
Rearranges the rows of an audit CSV to support different review workflows,
then writes the result back to the same file. The \code{sort_index} column
(set at write time) always allows restoration to report order.
}
\section{Verified status order for \code{by = "status"}}{

Rows are grouped: \code{NA} (unseen) → \code{"no_match"} → \code{"auto"} →
\code{"corrected"} → \code{"no"} → \code{"context"} → \code{"yes"}. Within each group,
rows are ordered by \code{sort_index}. This puts the most-needing-review
rows first.
}

\examples{
\dontrun{
# Group unverified rows first for review
crd_aud_sort("background/citation_audit.csv", by = "status")

# Restore report order when done
crd_aud_sort("background/citation_audit.csv", by = "report")
}
}
