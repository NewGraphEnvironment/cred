% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_write}
\alias{crd_aud_write}
\title{Write a citation audit CSV scaffold from a directory of Rmd files}
\usage{
crd_aud_write(
  rmd_dir = ".",
  out_file = "citation_audit.csv",
  pattern = "^[0-9]{4}-.*\\\\.Rmd$",
  exclude = "(references|session-info|report-change-log)"
)
}
\arguments{
\item{rmd_dir}{\code{character(1)} directory containing Rmd files.
Default \code{"."}.}

\item{out_file}{\code{character(1)} path for the output CSV.
Default \code{"citation_audit.csv"}.}

\item{pattern}{\code{character(1)} regex to select Rmd files.
Default \code{"^[0-9]{4}-.*\\\\.Rmd$"}.}

\item{exclude}{\code{character(1)} regex for files to skip.
Default skips references, session-info, and change-log chapters.}
}
\value{
Invisibly returns the written \link[tibble:tibble]{tibble}.
}
\description{
Scans all chapter Rmd files (those beginning with four digits), extracts
every inline citation with paraphrase context, and writes a CSV with blank
columns for manual verification.
}
\examples{
\dontrun{
crd_aud_write(rmd_dir = ".", out_file = "background/citation_audit.csv")
}
}
