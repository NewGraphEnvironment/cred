% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_eval_inline}
\alias{crd_aud_eval_inline}
\title{Fill quote and verified for NA rows using Zotero abstract text}
\usage{
crd_aud_eval_inline(audit_file, env = parent.frame(), overwrite = FALSE)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV.}

\item{env}{\code{environment} in which to evaluate inline R expressions.
Default \code{parent.frame()} — call from a session where project variables
are defined and they will be found automatically.}

\item{overwrite}{\code{logical(1)} if \code{TRUE}, re-evaluate rows that already have
\code{paraphrase_eval}. Default \code{FALSE}.}
}
\value{
The updated audit data frame, invisibly. Writes to \code{audit_file}.
}
\description{
For rows where \code{verified} is \code{NA} (no source file attached in Zotero),
Evaluate inline R expressions in audit paraphrases
}
\details{
Paraphrases extracted from Rmd source often contain unevaluated inline R
expressions such as \code{`r format(optimum_morice, big.mark = ",")`}.
These make paraphrase vs quote comparison difficult in the review app, and
prevent \code{\link[=crd_aud_score]{crd_aud_score()}} from detecting numeric mismatches.

\code{crd_aud_eval_inline()} evaluates each expression in \code{env} and stores the
result in a \code{paraphrase_eval} column. On evaluation failure the expression
is left as-is and a warning is issued. \code{paraphrase} is never modified —
\code{paraphrase_eval} is display-only.
}
\examples{
\dontrun{
# In a session where project variables are loaded:
optimum_morice <- 18175
crd_aud_eval_inline("background/citation_audit.csv")
}
}
