% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{crd_store_build}
\alias{crd_store_build}
\title{Build a ragnar evidence store from Zotero PDFs}
\usage{
crd_store_build(
  store_path,
  collection = NULL,
  citation_keys = NULL,
  model = "nomic-embed-text",
  zotero_dir = "~/Zotero",
  overwrite = FALSE
)
}
\arguments{
\item{store_path}{\code{character(1)} path for the \code{.duckdb} store to create.}

\item{collection}{\code{character(1)} Zotero collection name. Supply exactly one
of \code{collection} or \code{citation_keys}.}

\item{citation_keys}{\code{character} vector of Better BibTeX citation keys.}

\item{model}{\code{character(1)} Ollama embedding model.
Default \code{"nomic-embed-text"} — the model the shared stores are built with.}

\item{zotero_dir}{\code{character(1)} Zotero data directory. Default \code{"~/Zotero"}.}

\item{overwrite}{\code{logical(1)} replace an existing store. Default \code{FALSE}.}
}
\value{
Invisibly, a \link[tibble:tibble]{tibble} with one row per ingested
source (\code{citation_key}, \code{src_path}). Prints document and chunk counts.
}
\description{
Ingests the PDFs attached to a Zotero collection — or to an explicit set of
citation keys — into a ragnar DuckDB store, chunked, embedded and indexed
for both semantic and BM25 retrieval.
}
\details{
The embedding model is pinned explicitly on every build. This matters more
than it looks: \code{\link[ragnar:embed_ollama]{ragnar::embed_ollama()}} defaults to \code{embeddinggemma}, so a
store built without pinning is silently incomparable with every other store
in the shared corpus while appearing entirely healthy.

Building is expensive and machine-local. Sharing a built store is done out
of band; see \code{\link[=crd_store_connect]{crd_store_connect()}} for the retrieval side.
}
\examples{
\dontrun{
crd_store_build("data/rag/vca_refs.duckdb", collection = "vca")

crd_store_build(
  "data/rag/adhoc.duckdb",
  citation_keys = c("hall_etal2007Predictingriver", "beechie_etal2005ClassificationHabitat")
)
}
}
