% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_fill_src}
\alias{crd_aud_fill_src}
\title{Fill audit CSV quote and location columns from a source document}
\usage{
crd_aud_fill_src(
  audit_file,
  src,
  citation_key,
  n_results = 3L,
  min_score = 0.2,
  overwrite_verified = FALSE
)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV produced by
\code{\link[=crd_aud_write]{crd_aud_write()}}.}

\item{src}{\code{data.frame} as returned by \code{\link[=crd_docx_ext_txt]{crd_docx_ext_txt()}} or
\code{\link[=crd_pdf_ext_txt]{crd_pdf_ext_txt()}}. Source type is inferred from column names
(\code{doc_index} = docx, \code{page} = pdf).}

\item{citation_key}{\code{character(1)} citation key to process. Rows whose
\code{citation_key} column equals this value are searched.}

\item{n_results}{\code{integer(1)} number of top passages to return per row.
The top-scoring passage is written to \code{quote}; all candidates are stored
as JSON in \code{candidate_quotes}. Default \code{3L}.}

\item{min_score}{\code{numeric(1)} minimum token-match score to accept as a
match. Default \code{0.2}.}

\item{overwrite_verified}{\code{logical(1)} if \code{TRUE}, re-fill rows even if
\code{verified} is already set. Default \code{FALSE}.}
}
\value{
Invisibly returns the updated audit \link[tibble:tibble]{tibble}.
}
\description{
For each unverified row matching \code{citation_key}, searches \code{src} (a tibble
from \code{\link[=crd_docx_ext_txt]{crd_docx_ext_txt()}} or \code{\link[=crd_pdf_ext_txt]{crd_pdf_ext_txt()}}) for the best-matching
passage using the \code{paraphrase} column as the query. Fills:
\itemize{
\item \code{quote} — best-matching passage text
\item \code{page_or_section} — \code{doc_index} (docx) or \code{page} (pdf) of the match
\item \code{verified} — \code{"auto"} when a match meets \code{min_score}; \code{"no_match"} otherwise
}
}
\details{
Rows where \code{verified} is already non-blank are skipped unless
\code{overwrite_verified = TRUE}. The updated CSV is written back to \code{audit_file}.
}
\examples{
\dontrun{
doc <- crd_docx_ext_txt("background/price_etal2026.docx")
crd_aud_fill_src("background/citation_audit.csv", doc,
                 citation_key = "price_etal2026rebuildinggiis")
}
}
