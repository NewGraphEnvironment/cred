% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{crd_aud_verify_all}
\alias{crd_aud_verify_all}
\title{Verify all citation keys in an audit CSV against their source documents}
\usage{
crd_aud_verify_all(
  audit_file,
  sources = NULL,
  zotero_dir = "~/Zotero",
  min_score = 0.2,
  overwrite_verified = FALSE
)
}
\arguments{
\item{audit_file}{\code{character(1)} path to the audit CSV.}

\item{sources}{\code{data.frame} as returned by \code{\link[=crd_zot_src_lookup]{crd_zot_src_lookup()}}, with
columns \code{citation_key}, \code{src_path}, \code{src_type}. If \code{NULL} (default),
all unique keys in \code{audit_file} are looked up automatically via
\code{\link[=crd_zot_src_lookup]{crd_zot_src_lookup()}}.}

\item{zotero_dir}{\code{character(1)} Zotero data directory, passed to
\code{\link[=crd_zot_src_lookup]{crd_zot_src_lookup()}} when \code{sources} is \code{NULL}. Default \code{"~/Zotero"}.}

\item{min_score}{\code{numeric(1)} passed to \code{\link[=crd_aud_fill_src]{crd_aud_fill_src()}}.
Default \code{0.2}.}

\item{overwrite_verified}{\code{logical(1)} passed to \code{\link[=crd_aud_fill_src]{crd_aud_fill_src()}}.
Default \code{FALSE}.}
}
\value{
Invisibly returns the final audit \link[tibble:tibble]{tibble}.
}
\description{
Convenience wrapper around \code{\link[=crd_zot_src_lookup]{crd_zot_src_lookup()}} and \code{\link[=crd_aud_fill_src]{crd_aud_fill_src()}}.
For each citation key in the audit CSV that has a resolvable Zotero
attachment, loads the source document once and fills \code{quote},
\code{page_or_section}, and \code{verified} columns for all matching rows.
}
\details{
Keys with no Zotero attachment are skipped with a warning. Keys already
fully verified are skipped unless \code{overwrite_verified = TRUE}.
}
\examples{
\dontrun{
crd_aud_verify_all("background/citation_audit.csv")
}
}
