% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{crd_store_connect}
\alias{crd_store_connect}
\title{Connect to a ragnar evidence store, pulling and verifying it if needed}
\usage{
crd_store_connect(
  store,
  source = getOption("cred.store_source"),
  dir = "data/rag",
  profile = Sys.getenv("AWS_PROFILE"),
  read_only = TRUE,
  verify = TRUE
)
}
\arguments{
\item{store}{\code{character(1)} store name (e.g. \code{"vca_refs"}), or a path to an
existing \code{.duckdb} file.}

\item{source}{\code{character(1)} source URI holding the stores and \code{log.json}.
Defaults to \code{getOption("cred.store_source")}, then \code{CRED_STORE_SOURCE}.}

\item{dir}{\code{character(1)} local directory holding stores.
Default \code{"data/rag"}. Ignored when \code{store} is itself a path.}

\item{profile}{\code{character(1)} AWS profile. Default \code{AWS_PROFILE}.}

\item{read_only}{\code{logical(1)} open the store read-only. Default \code{TRUE}.}

\item{verify}{\code{logical(1)} check the local MD5 against the manifest.
Default \code{TRUE}. \code{FALSE} opens a local store without contacting \code{source} —
the only supported way to work fully offline.}
}
\value{
A \code{ragnar} store object, as returned by
\code{\link[ragnar:ragnar_store_create]{ragnar::ragnar_store_connect()}}.
}
\description{
Resolves a store by name, using the local copy when its MD5 matches the
shared manifest and downloading it from \code{source} otherwise. Verification is
the point: a store built against a different embedding model answers
differently while looking perfectly healthy, so a silent local rebuild
produces an artefact that is present but not trustworthy.
}
\details{
\code{source} has \strong{no default value}. Configure it with
\code{options(cred.store_source = )} or the \code{CRED_STORE_SOURCE} environment
variable, in the shape \verb{s3://<bucket>/<prefix>/}. Pushing a store is out of
scope for this package — it is a build-side operation performed rarely by
whoever built the store.
}
\examples{
\dontrun{
options(cred.store_source = "s3://<bucket>/<prefix>/")
store <- crd_store_connect("vca_refs")
crd_search(store, "bankfull width regression")

# Offline, against a store already on disk
crd_store_connect("vca_refs", verify = FALSE)
}
}
