% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentence.R
\name{crd_sent_ext_key}
\alias{crd_sent_ext_key}
\title{Extract the sentence(s) containing a specific citation key}
\usage{
crd_sent_ext_key(line, key, min_chars = 40L)
}
\arguments{
\item{line}{\code{character(1)} a single line of Rmd prose.}

\item{key}{\code{character(1)} BBT citation key (without leading \code{@}).}

\item{min_chars}{\code{integer(1)} minimum fragment length before falling
back to the full line. Default \code{40L}.}
}
\value{
\code{character(1)} trimmed sentence fragment, or full line when
the fragment is uninformative.
}
\description{
Splits a line of Rmd prose into sentence fragments and returns the
fragment(s) that contain \verb{@key}. Falls back to the full trimmed line
when the extracted fragment is too short or starts mid-sentence
(lowercase first character).
}
\details{
\strong{Split rules} (applied outside \verb{[...]} citation bracket groups):
\itemize{
\item \code{. [Capital]} — period followed by a capital letter, excluding
common abbreviations (\verb{et al.}, \code{Dr.}, \code{Mr.}, \code{St.}).
\item \code{. @key} — period followed by a narrative citation, so that
sentences of the form \verb{@author documented...} are correctly
isolated.
}

The lookahead \verb{(?![^[]*\])} prevents splitting inside \verb{[@k1; @k2]}
by checking that a \verb{]} is not reachable without first crossing a \code{[}.
}
\examples{
line <- paste(
  "@smith2020 documented X; @jones2021 confirmed Y.",
  "@brown2022 extended the finding."
)
crd_sent_ext_key(line, "jones2021")
crd_sent_ext_key(line, "brown2022")
}
