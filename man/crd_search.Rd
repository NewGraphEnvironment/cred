% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{crd_search}
\alias{crd_search}
\title{Search a ragnar evidence store for passages supporting a claim}
\usage{
crd_search(
  store,
  query,
  top_k = 5L,
  method = c("hybrid", "bm25", "vss"),
  zotero_dir = "~/Zotero"
)
}
\arguments{
\item{store}{a ragnar store, from \code{\link[=crd_store_connect]{crd_store_connect()}} or
\code{\link[ragnar:ragnar_store_create]{ragnar::ragnar_store_connect()}}.}

\item{query}{\code{character(1)} search text.}

\item{top_k}{\code{integer(1)} passages to retrieve \strong{per method}. Default \code{5L}.
Under \code{method = "hybrid"} the vector and lexical result sets are unioned and
then adjacent chunks are merged, so the number of rows returned is neither
\code{top_k} nor \code{2 * top_k} — expect somewhere between the two.}

\item{method}{\code{character(1)} one of \code{"hybrid"}, \code{"bm25"}, \code{"vss"}.}

\item{zotero_dir}{\code{character(1)} Zotero data directory used to resolve
citation keys. Default \code{"~/Zotero"}.}
}
\value{
A \link[tibble:tibble]{tibble} ordered best-match first, with columns:
\itemize{
\item \code{citation_key} (\code{character}) — BBT key, \code{NA} if unresolvable.
\item \code{origin} (\code{character}) — source path recorded in the store.
\item \code{chunk_id}, \code{start}, \code{end} (\code{integer}) — location within the document.
A returned passage may be several adjacent chunks merged into one, in
which case \code{chunk_id} is the first of them and \code{start}/\code{end} span them all.
\item \code{text} (\code{character}) — the retrieved passage, verbatim.
\item \code{score} (\code{numeric}) — retrieval metric value. Where a passage merges
several chunks this is the best score among them: highest for \code{bm25},
lowest for \code{cosine_distance}.
\item \code{metric} (\code{character}) — which metric produced \code{score} (\code{"bm25"} or
\code{"cosine_distance"}). Scores are comparable only within a metric.
\item \code{method} (\code{character}) — the method actually used, which differs from
the request when a fallback occurred.
}
}
\description{
Retrieves the passages most relevant to \code{query} and labels each with the
citation key of the paper it came from, so a result can be cited directly
rather than chased back through a file path.
}
\details{
Unlike the token-overlap search in \code{\link[=crd_pdf_srch_clm]{crd_pdf_srch_clm()}}, which scores one
known source against one paraphrase, this searches an entire indexed corpus.

\code{method = "hybrid"} combines semantic (vector) and lexical (BM25) retrieval
and needs a running Ollama instance to embed the query. When Ollama is
unreachable the search \strong{falls back to BM25 with a warning} rather than
failing: lexical retrieval needs no embedding and remains effective for the
numeric and parameter-level claims this package exists to check.
}
\examples{
\dontrun{
store <- crd_store_connect("vca_refs")
crd_search(store, "bankfull width regression drainage area precipitation")
}
}
